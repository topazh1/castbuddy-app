# Temporary probe (manual only): ask the Supabase project which auth providers
# are actually enabled, so the app can show only buttons that work.
name: Auth probe

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: auth settings
        run: |
          curl -sS -m 20 -w '\nSTATUS=%{http_code}\n' \
            'https://dnbtpzguoudkjqvlmoau.supabase.co/auth/v1/settings' \
            -H 'apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRuYnRwemd1b3Vka2pxdmxtb2F1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODYwMjkzMTcsImV4cCI6MjEwMTYwNTMxN30.kISyxdLzw_j__8bCdiDkA8e9oCqbOOXcnNDAetrKLYM'
